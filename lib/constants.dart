import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color.fromRGBO(20, 20, 20, 1); // Very back
  static const Color surface = Color(0xFF212121); // Toggle track
  static const Color element = Color(0xFF2E2E2E); // Active toggle button
  static const Color textPrimary = Color(0xFFE6E6E6);
  static const Color textSecondary = Color(0xFF999999);

  static const Color neonGreen = Color.fromRGBO(52, 203, 54, 1);
  static const Color accentPink = Color.fromRGBO(224, 82, 160, 1);
}
